// NOTE: only the subtitle text changed to avoid confusion.
// Replace just this line in your existing file if you don't want to overwrite:
//  메일 1건(요청 묶음) → Quotation 1건으로 등록
// with:
//  메일/요청 1건(한 묶음) → Quotation 1건으로 등록
